% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_data.R
\name{generate_mock_fgt_dataset}
\alias{generate_mock_fgt_dataset}
\title{Generate a complete set of mock data for all tools}
\usage{
generate_mock_fgt_dataset(
  n_samples = 10,
  n_sequences = 1000,
  n_genes = 1000,
  sample_id_prefix = "Sample",
  cst_distribution = NULL,
  seed = NULL,
  create_files = FALSE,
  output_dir = NULL
)
}
\arguments{
\item{n_samples}{Number of samples to generate}

\item{n_sequences}{Number of sequences for SpeciateIT data}

\item{n_genes}{Number of genes for VIRGO data}

\item{sample_id_prefix}{Prefix for sample IDs}

\item{cst_distribution}{Named vector of CST probabilities}

\item{seed}{Random seed for reproducibility}

\item{create_files}{Logical; whether to write files to disk}

\item{output_dir}{Directory to write files to (if create_files=TRUE)}
}
\value{
A list with components for each tool's data
}
\description{
Creates a coordinated set of mock data for SpeciateIT, VIRGO, and VALENCIA
with consistent samples and community state types.
}
\examples{
\donttest{
# Generate a complete set of mock data
mock_data <- generate_mock_fgt_dataset(
  n_samples = 10,
  n_sequences = 100,
  n_genes = 500
)

# Generate files on disk
temp_dir <- tempdir()
mock_data_with_files <- generate_mock_fgt_dataset(
  n_samples = 5,
  create_files = TRUE,
  output_dir = temp_dir
)
}
}
