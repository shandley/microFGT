% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_data.R
\name{generate_mock_speciateit}
\alias{generate_mock_speciateit}
\title{Generate mock SpeciateIT results}
\usage{
generate_mock_speciateit(
  n_sequences = 100,
  seq_id_prefix = "Seq",
  species_distribution = NULL,
  confidence_range = c(0.7, 1),
  decisions_range = c(5, 20),
  include_unclassified = 0.05,
  seed = NULL
)
}
\arguments{
\item{n_sequences}{Number of sequences to generate}

\item{seq_id_prefix}{Prefix for sequence IDs}

\item{species_distribution}{Named vector of probabilities for different species.
Names should be species names, values should be probabilities that sum to 1.}

\item{confidence_range}{Range of confidence values (posterior probabilities) to generate}

\item{decisions_range}{Range of decision counts to generate}

\item{include_unclassified}{Proportion of sequences that should be unclassified (0-1)}

\item{seed}{Random seed for reproducibility}
}
\value{
A data frame with the columns: "Sequence ID", "Classification",
"posterior probability", and "number of Decisions"
}
\description{
Creates simulated speciateIT classification results for testing purposes.
The function generates data that mimics the format of the "MC_order7_results.txt"
file produced by speciateIT.
}
\examples{
\donttest{
# Generate default mock data
mock_data <- generate_mock_speciateit(n_sequences = 100)

# Generate data with custom species distribution
species_dist <- c(
  "Lactobacillus crispatus" = 0.5,
  "Lactobacillus iners" = 0.3, 
  "Gardnerella vaginalis" = 0.2
)
mock_data <- generate_mock_speciateit(
  n_sequences = 100, 
  species_distribution = species_dist
)
}
}
