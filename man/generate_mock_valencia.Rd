% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_data.R
\name{generate_mock_valencia}
\alias{generate_mock_valencia}
\title{Generate mock VALENCIA results}
\usage{
generate_mock_valencia(
  n_samples = 20,
  sample_id_prefix = "Sample",
  cst_distribution = NULL,
  include_scores = TRUE,
  add_taxonomic_data = TRUE,
  seed = NULL
)
}
\arguments{
\item{n_samples}{Number of samples to generate}

\item{sample_id_prefix}{Prefix for sample IDs}

\item{cst_distribution}{Named vector of CST probabilities}

\item{include_scores}{Logical; whether to include CST probability scores}

\item{add_taxonomic_data}{Logical; whether to add taxonomic abundance data}

\item{seed}{Random seed for reproducibility}
}
\value{
A list with components:
\itemize{
\item \code{cst}: Data frame with CST assignments
\item \code{scores}: Matrix of CST probability scores (if include_scores=TRUE)
\item \code{abundance}: Data frame with taxonomic abundances (if add_taxonomic_data=TRUE)
}
}
\description{
Creates simulated VALENCIA community state type (CST) classifications
for testing purposes.
}
\examples{
\donttest{
# Generate default mock VALENCIA data
valencia_data <- generate_mock_valencia(n_samples = 20)

# Generate data with specific CST distribution
cst_dist <- c("I" = 0.6, "III" = 0.3, "IV" = 0.1)
valencia_data <- generate_mock_valencia(
  n_samples = 10,
  cst_distribution = cst_dist,
  include_scores = TRUE,
  add_taxonomic_data = TRUE
)
}
}
