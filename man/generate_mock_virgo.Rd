% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_data.R
\name{generate_mock_virgo}
\alias{generate_mock_virgo}
\title{Generate mock VIRGO results}
\usage{
generate_mock_virgo(
  n_genes = 1000,
  n_samples = 10,
  sample_id_prefix = "Sample",
  include_zeros = 0.7,
  abundance_distribution = "lognormal",
  abundance_params = c(1, 2),
  simulation_type = "realistic",
  cst_distribution = NULL,
  seed = NULL
)
}
\arguments{
\item{n_genes}{Number of genes to include (VIRGO contains ~118K genes)}

\item{n_samples}{Number of samples to generate}

\item{sample_id_prefix}{Prefix for sample IDs}

\item{include_zeros}{Proportion of zero counts to include (sparsity)}

\item{abundance_distribution}{Distribution to use for non-zero counts ("lognormal" or "zipf")}

\item{abundance_params}{Parameters for the abundance distribution
\itemize{
\item For "lognormal": c(meanlog, sdlog)
\item For "zipf": c(alpha)
}}

\item{simulation_type}{Type of simulation to perform:
\itemize{
\item "realistic": Realistic vaginal microbiome with CST structure
\item "random": Random gene abundances without structure
}}

\item{cst_distribution}{Named vector of CST probabilities (only for realistic simulation)}

\item{seed}{Random seed for reproducibility}
}
\value{
A list with components:
\itemize{
\item \code{counts}: Matrix of gene counts (genes × samples)
\item \code{genes}: Data frame with gene information
\item \code{metadata}: Data frame with sample metadata
}
}
\description{
Creates simulated VIRGO gene abundance results for testing purposes.
The function generates data that mimics the format of the VIRGO output files.
}
\examples{
\donttest{
# Generate default mock VIRGO data
virgo_data <- generate_mock_virgo(n_genes = 1000, n_samples = 10)

# Generate data with specific CST distribution
cst_dist <- c("CST I" = 0.5, "CST III" = 0.3, "CST IV" = 0.2)
virgo_data <- generate_mock_virgo(
  n_genes = 1000, 
  n_samples = 20,
  simulation_type = "realistic",
  cst_distribution = cst_dist
)
}
}
